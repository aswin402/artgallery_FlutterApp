import 'package:flutter/material.dart';

class AppColors {
  // ===== Light Theme =====
  static const background = Color(0xFFF8F9FB);
  static const foreground = Color(0xFF2A2F3A);

  static const card = Color(0xFFFFFFFF);
  static const cardForeground = foreground;

  static const popover = Color(0xFFFFFFFF);
  static const popoverForeground = foreground;

  static const primary = Color.fromARGB(255, 47, 194, 165); // teal-blue
  static const primaryForeground = Color(0xFFF0FDFF);

  static const secondary = Color(0xFFF1F3F7);
  static const secondaryForeground = Color(0xFF3A3F4A);

  static const muted = Color(0xFFF0F2F6);
  static const mutedForeground = Color(0xFF8A90A0);

  static const accent = Color(0xFFE9EEF5);
  static const accentForeground = Color(0xFF3A3F4A);

  static const destructive = Color(0xFFE5484D);
  static const destructiveForeground = Color(0xFFFFF5F5);

  static const border = Color(0xFFE5E7EB);
  static const input = Color(0xFFE5E7EB);
  static const ring = Color.fromARGB(255, 12, 170, 127);

  // ===== Dark Theme =====
  static const darkBackground = Color.fromARGB(255, 12, 12, 12);
  static const darkForeground = Color(0xFFF9FAFB);
  static const darkNavbar = Color.fromARGB(255, 4, 4, 4);
  static const darkCard = Color.fromARGB(255, 28, 28, 31);
  static const darkBorder = Color.fromARGB(51, 74, 70, 70);
}

